<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

/**
 * Demonstration data — Filipino clinic staff, clients, patients, products,
 * appointments, consults, invoices and payments. Fleshed out per build phase.
 */
class DemoSeeder extends Seeder
{
    public function run(): void
    {
        // Populated as modules land (see plan phase 12).
    }
}
